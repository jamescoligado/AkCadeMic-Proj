@model IEnumerable<AkCadeMic.Models.ThreadO>

@{
    ViewData["Title"] = "Manage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="text-center">
    <h1>Manage Other Threads</h1>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                Post Id
            </th>
            <th>
                Subject
            </th>
            <th>
                Body
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PostIdO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubjectO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BodyO)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PostIdO">Edit</a>
                <a asp-action="Delete" asp-route-id="@item.PostIdO"
                   onclick='return confirm("Are you sure?");'>Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>
