@model IEnumerable<AkCadeMic.Models.ReplyS>

@{
    ViewData["Title"] = "ManageComments";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="text-center">
    <h1>Manage STEM Comments</h1>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Post Id
                </th>
                <th>
                    Email
                </th>
                <th>
                    Text
                </th>
                <th>
                    Action
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ThreadIdS)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EmailS)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TextS)
                </td>
                <td>
                    <a asp-action="DeleteComment" asp-route-id="@item.IdS">Delete</a>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>
